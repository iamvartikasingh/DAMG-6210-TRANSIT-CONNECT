CREATE OR REPLACE VIEW WEEKLY_BOOKING_TRENDS AS
SELECT 
    TO_CHAR(BOOKING_DATE, 'IW') AS WEEK_NUMBER,
    COUNT(*) AS TOTAL_BOOKINGS,
    SUM(FINAL_TOTAL_AMOUNT) AS TOTAL_REVENUE
FROM 
    BOOKING
GROUP BY 
    TO_CHAR(BOOKING_DATE, 'IW')
ORDER BY 
    WEEK_NUMBER;